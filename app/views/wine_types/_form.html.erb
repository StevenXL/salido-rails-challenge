<%= form_for @wine_type  do |f| %>
  <% if @wine_type.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@wine_type.errors.count, "error") %> prohibited this wine type from being saved:</h2>

        <ul>
        <% @wine_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit %>
  </div>
<% end %>
