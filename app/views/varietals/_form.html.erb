<%= form_for @varietal do |f| %>
  <% if @varietal.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@varietal.errors.count, "error") %> prohibited this varietal from being saved:</h2>

        <ul>
        <% @varietal.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label "Select Wine Type" %>
    <%= f.select :wine_type_id, WineType.all.collect {|w| [w.name, w.id]}, {include_blank: false}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit %>
  </div>
<% end %>
